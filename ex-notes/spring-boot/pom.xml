<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>top.yooonn.explore.notes</groupId>
    <artifactId>ex-notes</artifactId>
    <version>${revision}</version>
  </parent>

  <artifactId>spring-boot</artifactId>
  <name>::notes::spring boot</name>
  <packaging>pom</packaging>

  <properties>
    <main.basedir>${project.basedir}/../..</main.basedir>
    <doc.name>spring-boot</doc.name>
  </properties>
  <scm>
    <connection>${git.connection}</connection>
    <developerConnection>${git.developerConnection}</developerConnection>
    <url>${git.url}</url>
  </scm>

  <modules>
    <module>data</module>
    <module>foo-app</module>
    <module>freemarker</module>
    <module>kafka</module>
    <module>mybatisplus</module>
    <module>spring-shell</module>
    <module>thymeleaf</module>
    <module>web</module>
    <module>xxl-job</module>
    <module>zookeeper-dubbo-consumer</module>
    <module>zookeeper-dubbo-provider</module>
  </modules>
</project>
